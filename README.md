# homebrew-tap

Homebrew tap for duailibe's tools.

## Usage

```bash
brew tap duailibe/tap
brew install linear-cli
```

Or install directly:

```bash
brew install duailibe/tap/linear-cli
```

## Available Formulas

- **linear-cli** - A fast, no-nonsense CLI for Linear
